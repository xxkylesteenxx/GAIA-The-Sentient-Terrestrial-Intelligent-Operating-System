# GAIA Core Dependencies
# Phase 1 Build - Updated 2026-02-28
# CORRECTED: Fixed anthropicsdk→anthropic, swisseph→pyswisseph
# ADDED: click, rich, sentence-transformers (missing from original)

# WebSocket API
websockets>=12.0
aiohttp>=3.9.0

# Scientific Computing (Z-score calculation)
numpy>=1.24.0
scipy>=1.11.0

# Avatar & LLM Integration
openai>=1.0.0
anthropic>=0.20.0  # CORRECTED: was anthropicsdk (doesn't exist on PyPI)
chromadb>=0.4.22
sentence-transformers>=2.2.0  # ADDED: missing dependency

# CLI Interface
click>=8.1.0  # ADDED: used in overlay/cli.py
rich>=13.0.0  # ADDED: used in overlay/cli.py

# Astrology (natal charts)
kerykeion>=4.0.0
pyswisseph>=2.10.3  # CORRECTED: was swisseph (wrong package name)

# Cryptography (memory encryption)
cryptography>=41.0.0

# Configuration
pyyaml>=6.0.0
python-dotenv>=1.0.0
